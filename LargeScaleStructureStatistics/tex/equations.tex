\documentclass[12pt]{article}
\usepackage{amsmath}
\usepackage{xcolor}

\definecolor{dkyellow}{rgb}{0.62,0.62,0.10}
\definecolor{de}{rgb}{0.4,0.4,0.7}
\newcommand{\dz}{{\color{red}\Delta z}}
\newcommand{\dth}{{\color{blue}\Delta \theta}}
\newcommand{\z}{{\color{white} z}}

\providecommand{\vecsymbol}[1]{\ensuremath{\boldsymbol{#1}}}
\providecommand{\rv}{\vecsymbol{r}}

%\pagecolor{white}
\color{black}
\begin{document}

$$
\delta_g(\rv) = \frac{n_g(\rv) - \langle n_g\rangle}{\langle n_g\rangle}
$$

$$
\delta_m(\rv) = \frac{\rho_m(\rv) - \langle \rho_m\rangle}{\langle \rho_m\rangle}
$$

$$
\delta_g(\rv) = b_g \delta_m(\rv)
$$

$$
\left( 2(n-1) \right)^{-1/2}
$$

%\newpage
%\pagecolor{black}
%\color{white}

%$$
%\color{red}
%b_x \beta_x = f(z) \simeq \Omega_m(z)^{\gamma(w)}
%\simeq \left[ \frac{\Omega_{m,0}(1+z)^3}{1 - \Omega_{m,0} + \Omega_{m,0}(1+z)^3}\right]^{0.55}
%$$

\end{document}
